spring.application.name=galapagos-booking

# Conexión a Neon PostgreSQL
# Usamos el pooler de Neon (recomendado para aplicaciones web)
spring.datasource.url=jdbc:postgresql://ep-billowing-river-al4gfg4u-pooler.c-3.eu-central-1.aws.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_pLRt9Usrqe3E
spring.datasource.driver-class-name=org.postgresql.Driver

# Dialecto para PostgreSQL (Fundamental cambiar de MariaDB a PostgreSQL)
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ddl-auto en 'update'
spring.jpa.hibernate.ddl-auto=update

# Control de logs (show-sql=true solo para desarrollo, en producción mejor false)
spring.jpa.show-sql=true

# Configuración de archivos
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
server.tomcat.max-swallow-size=10MB